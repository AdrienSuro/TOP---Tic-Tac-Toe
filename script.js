const playerFactory = (name) => {
  let score = 0;
  // où créer une variable qui va contenir les messages du type `${name} has 4 points` ???
  return { name, currentScore };
};

const gameboardModule = (() => {
  let 
})();
